# Expense Analyzer

Reads a CSV of expenses (`date,category,amount,description`) and prints a report: total spent, spend by category (sorted), and the largest single expense.

## Usage

Place your CSV as `somedata.csv`, then:

```bash
python analyzer.py
```

## Modules

`csv`